/** User-facing application name. Single source of truth for branding strings. */
export const APP_NAME = 'RayColl';

/** How many study reviews to batch before flushing to cloud. */
export const STUDY_REVIEW_FLUSH_THRESHOLD = 10;

/** Debounce window in ms before a cloud persistence snapshot is flushed. */
export const PERSISTENCE_DEBOUNCE_MS = 1200;
