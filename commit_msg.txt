Allow changing step type when editing study mode steps

- Enabled changing the step type during study mode step editing
- Automatically resets other parameters to defaults (preserving page number) when switching types
- Restores original parameters if user switches back to the initial step type
- Added test coverage in AddStepDialog tests for step type transitions
