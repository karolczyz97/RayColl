rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }

    match /users/{uid} {
      allow read, write: if isOwner(uid);

      match /decks/{deckId} {
        allow read, write: if isOwner(uid);

        match /cards/{cardId} {
          allow read, write: if isOwner(uid);
        }
      }

      match /studyModes/{modeId} {
        allow read, write: if isOwner(uid);
      }

      match /activity/{date} {
        allow read, write: if isOwner(uid);
      }
    }
  }
}
